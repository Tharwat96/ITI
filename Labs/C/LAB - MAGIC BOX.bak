#include <stdio.h>
#include <conio.h>

int main() 
{
   int size;
	printf("Please enter the size of the magic box(odd number)");
   scanf("%d", &size);
   if(size%2==0)
   {
   	printf("You entered an even number, please re-enter the size");
      scanf("%d", &size);

   }
   getch();
	return 0;
}

void MagicBox(int s)
{


}
